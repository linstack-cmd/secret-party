{
  "name": "secret-party-monorepo",
  "version": "0.0.0",
  "description": "Secret Party Monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @secret-party/dashboard --filter @secret-party/database -r --parallel dev",
    "build": "pnpm --filter @secret-party/dashboard build",
    "start": "pnpm --filter @secret-party/database migrate && pnpm --filter @secret-party/dashboard start",
    "test": "pnpm --filter @secret-party/dashboard test"
  },
  "author": "Danny Kim",
  "license": "MIT",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "drizzle-orm": "^0.44.5",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
